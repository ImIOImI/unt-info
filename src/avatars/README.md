Player portraits, one per file, named for the `avatar` stem in
../content/legions.ts. Any of .png .jpg .jpeg .webp. A missing file just
renders a lettered tile instead, so the page never breaks on one.
